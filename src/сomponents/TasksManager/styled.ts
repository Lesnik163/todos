import { Box } from "@mui/material";

import styled from 'styled-components';

export const StyledTaskManager = styled(Box)`
height: 50px;
padding: 0 10px;
`;

export const StyledButtonRow = styled(Box)`
display: flex;
justify-content: space-between;
width: 280px;
`;